name: flutter_instagram_storyboard
description: A package that allows you to create stories like in instagram
version: 2.0.2
homepage: https://github.com/caseyryan/flutter_instagram_storyboard

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get_storage:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

